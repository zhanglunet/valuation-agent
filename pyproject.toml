[project]
name = "valuation-agent"
version = "3.0.0"
description = "Hermes Agent skills for strategic valuation analysis: company-level + project-level cash-flow attribution"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["valuation_agent"]
